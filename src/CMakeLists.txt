project(imagine VERSION 0.0.1 LANGUAGES CXX)

find_package(SDL2 REQUIRED)

set(SOURCE_EXE
    main.cpp
)

add_executable(${PROJECT_NAME} ${SOURCE_EXE})

target_include_directories(${PROJECT_NAME} PRIVATE ${SDL2_INCLUDE_DIRS})# ${SDL2_IMAGE_INCLUDE_DIR} ${SDL2TTF_INCLUDE_DIR} ${LUA_INCLUDE_DIR})

target_link_libraries(${PROJECT_NAME} PRIVATE ${SDL2_LIBRARIES})# ${SDL2_IMAGE_LIBRARY} ${SDL2TTF_LIBRARY} ${LUA_LIBRARIES})

target_include_directories(${PROJECT_NAME}
    PRIVATE ${CMAKE_CURRENT_BINARY_DIR}
)

